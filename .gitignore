__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
tests/expected_outputs.json

# Rust
rust/target/
# Cargo.lock IS tracked: this crate ships a binary, so the lockfile is what makes
# a build reproducible. Only libraries leave it out.

# macOS
.DS_Store
